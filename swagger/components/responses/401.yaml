description: Unauthorized
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - error
      properties:
        status:
          type: integer
          example: 401
        message:
          type: string
          example: Token is expired!
        error:
          type: array
          example: []
