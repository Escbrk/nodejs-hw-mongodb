description: Invalid ID!
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - error
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: Invalid ID!
        error:
          type: array
          example: []
